# Web framework + WSGI server
flask>=3.0
gunicorn>=26.2.0
click==8.4.2

# Databricks SDK — required by demo scripts (launch_*_demo.py) and the
# sdp-meta CLI (`python -m databricks.labs.sdp_meta.cli`, installed from
# the wheel built by databricks_app/start.sh).
databricks-sdk>=0.25

# Config parsing used by onboarding, demo scripts, and parse-spec endpoint
PyYAML>=6.0
