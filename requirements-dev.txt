# Development and test dependencies for databricks-labs-sdp-meta.
#
# Mirrors DEV_REQUIREMENTS, IT_REQUIREMENTS, and MCP_REQUIREMENTS in setup.py.
# Keep this file in sync with setup.py.
#
# Usage:
#   pip install -r requirements-dev.txt

-r requirements.txt

# Install the project itself in editable mode so `from
# databricks.labs.sdp_meta...` resolves to the working tree
# (matches the namespace-package layout in setup.py).
-e .

# Test stack (DEV_REQUIREMENTS in setup.py)
flake8==7.3.0
delta-spark==3.0.0
pytest>=7.0.0
coverage>=7.15.4
pyspark==3.5.5

# Integration-test CLI (IT_REQUIREMENTS in setup.py)
typer[all]==0.27.1

# MCP server extra (MCP_REQUIREMENTS in setup.py)
mcp>=2.1.1,<3.0
