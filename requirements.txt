# Runtime dependencies for databricks-labs-sdp-meta.
#
# Mirrors INSTALL_REQUIRES in setup.py. Keep this file in sync with setup.py;
# setup.py is the source of truth when sdp-meta is installed as a wheel.
#
# Usage:
#   pip install -r requirements.txt
#
# For development and test dependencies, use requirements-dev.txt instead.

setuptools>=65,<83
databricks-sdk>=0.20,<1
PyYAML>=6.0,<7
